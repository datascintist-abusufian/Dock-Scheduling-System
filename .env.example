# Optional. When both are set the app persists to Supabase/PostgreSQL.
# When omitted, the app runs in "demo mode" against an in-memory store seeded
# with the representative records in src/lib/data/seed.ts.
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
